import React from 'react';
import { Row } from 'react-bootstrap';
import MovieSelected from '../MovieSelected/MovieSelected';

const MoviesContainer = () => (<Row><MovieSelected /></Row>);

export default MoviesContainer;
